{
  "name": "dsh-portable-app",
  "private": true,
  "version": "0.0.0",
  "description": "Portable DeepSeek Harness runtime: the npm-installed dsh CLI (updatable via pnpm).",
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.5-rc.2"
  }
}
